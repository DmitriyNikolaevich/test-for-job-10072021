export const getNameSelector = state => {
    return state.main.name
}

export const getHitsSelector = state => {
    return state.main.hits
}

export const getMissesSelector = state => {
    return state.main.misses
}